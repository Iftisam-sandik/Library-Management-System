---
- name: Restore missing Grafana folders and dashboards
  hosts: monitoring_servers
  become: true

  vars:
    grafana_service_account_token: >-
      {{ lookup('ansible.builtin.file',
      '/home/ubuntu/.ansible-secrets/grafana_service_account_token') | trim }}

  roles:
    - grafana_dashboards
